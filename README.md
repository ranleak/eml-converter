# eml-converter
Easily convert EML files to HTML and PDF quickly with a simple Python script.

Other than the dependencies shown in ```requirements.txt```, you will need ```wkhtmltopdf``` installed on your system:
```
sudo apt-get update
sudo apt-get install wkhtmltopdf
```

Enjoy!